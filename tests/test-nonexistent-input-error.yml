name: Test nonexistent input file error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with a nonexistent file path
        id: action
        uses: .
        continue-on-error: true
        with:
          inputs: "/tmp/this-file-does-not-exist-12345.txt"
          release-signing-artifacts: "false"

      - name: Assert action failed with expected error
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail when input file does not exist"
            exit 1
          fi
          echo "PASS: action correctly failed when input file does not exist"
